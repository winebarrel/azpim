coverage:
  status:
    project:
      default:
        target: 90%
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 0%

ignore:
  # kong wiring with nothing to assert that --help does not already prove.
  - "cmd/azpim/main.go"
