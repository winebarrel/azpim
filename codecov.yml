coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 1%
    patch:
      default:
        target: 95%
        threshold: 0%

ignore:
  # kong wiring with nothing to assert that --help does not already prove.
  - "cmd/azpim/main.go"
